# URL base da API Spring Boot (login JWT, chamadas futuras).
# Obrigatório incluir o protocolo (https://). Exemplo produção (Netlify):
# VITE_API_BASE_URL=https://sentineia.up.railway.app
VITE_API_BASE_URL=http://localhost:8080
