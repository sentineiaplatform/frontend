# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & package managers
node_modules

# Production / build output
dist
dist-ssr
build
storybook-static
*.local

# Vite (timestamps de lock do config)
vite.config*.timestamp*

# Coverage & reports
coverage
.nyc_output
*.lcov
playwright-report
test-results
.jest-cache

# TypeScript incremental
*.tsbuildinfo

# Linters / formatters
.eslintcache
.prettier-cache
.stylelintcache

# Env — segredos; use `!.env.example` se versionar apenas o template (`git check-ignore -v` para validar)
.env
!.env.example

# Turborepo / monorepo tool cache
.turbo

# OS
.DS_Store
.DS_Store?
Thumbs.db
ehthumbs.db
Desktop.ini
**/.DS_Store

# Certificates / keys gerados localmente
*.pem
*.key

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.cursor
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
